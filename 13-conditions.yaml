- name: check number of live sessions
  hosts: local
  connection: local
  vars:
    live_sessions: 6 #4 condition won't execute
  tasks:
  - name: is ansible more than 5 sessions
    debug:
      msg: "Yes, Ansible took more than 5 sessions"
    when: live_sessions > 5